# schema.graphql

type Query {
  getAllAnimals: [Animal]
}

type Animal {
  id: ID!
  name: String!
  fact: String
}

type User {
    id: ID!
    email: String!
    name: String!
    password: String!
}

type Mutation {
    addUser(email: String!, name: String, password: String): User
}



